#include "main.h"
#include <string.h>

/**
 * _strlen - return length of string
 * @s: the string we are checking
 *
 * Return:  length of string
 */

int _strlen(char *s)
{
	int i;

	i = strlen(str)
}
