#include "main.h"
#include <stdio.h>

/**
 * main - program entry point
 * @argc: The number of args
 * @argv: array of strings
 *
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
