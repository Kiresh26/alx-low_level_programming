#include "main.h"
#include <string.h>

/**
 * _strcat: concatenate two strings
 *
 * Return: 0
 */

char *_strcat(char *dest, char *src)
{
	dest_len = strlen(dest);

	for (i = 0; i < dest; i++)
		dest++;

	return (dest);

}

